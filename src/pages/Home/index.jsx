import Navbar from '../../components/Navbar';
import styles from './Home.module.scss';

const Home = () => {
	return (
		<div className={styles.App}>
			<Navbar />
		</div>
	);
};

export default Home;
