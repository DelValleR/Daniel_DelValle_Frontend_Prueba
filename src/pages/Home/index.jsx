import TopWrapper from '../../components/TopWrapper';
import styles from './Home.scss';

const Home = () => {
	return (
		<div className={styles.App}>
			<TopWrapper />
		</div>
	);
};

export default Home;
